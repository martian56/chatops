#!/bin/bash
# Pre-removal script for chatops-agent

# Stop the service before removal
if command -v systemctl >/dev/null 2>&1; then
    systemctl stop chatops-agent.service 2>/dev/null || true
fi

exit 0

